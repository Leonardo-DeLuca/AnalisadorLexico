void main {
    fallen, yurih, ks, kye, chelo: string;
    majors: integer;

    integer funcaoExemplo(float params) {
        teclado: string;
        precoSetup: float;

        string funcao2(string teste) {
            test: integer;
            variavelFuncao2: string;

            inicio;

            fim

            return ('')
        }

        inicio;

        fallen = "test";

        fim
        
        return (chelo)
    }

    inicio;

    if (majors == 2) {
        fallen = "ABC";
    };

    if (majors == 2) {
        ks = "ABC";
        majors = majors;
    };

    fim
}