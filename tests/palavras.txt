void main {
    fallen, yurih, ks, kye, chelo: string;
    majors: integer;

    integer funcaoExemplo(float params) {
        inicio;

        fim
        
        return (chelo)
    }

    inicio;

    if (majors == 2) {
        fallen = "ABC";
    };

    if (majors == 2) {
        ks = "ABC";
        majors = majors;
    };

    fim
}